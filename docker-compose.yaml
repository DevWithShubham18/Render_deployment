services:
  server:
    build: ./server
    image: chartify:latest
    container_name: chartify
    restart: unless-stopped

    ports:
      - "8080:8080"

    env_file:
      - ./server/.env


  client:
    build: ./client
    ports:
      - "3000:80"
    depends_on:
      - server